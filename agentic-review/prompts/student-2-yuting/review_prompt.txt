Examine the microservice for warehouse management. Verify whether the database-per-service requirement was truly adhered to, meaning the backend only accesses the database API and never directly interacts with SQLite. Additionally, confirm that the AI-Mode functionality that calls Ollama is genuine and not only a placeholder. Give one detailed observation, limit it to sixty words, and only make use of the supporting data.
